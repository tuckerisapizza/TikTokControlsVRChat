# TikTokControlsVRChat

Allows TikTok Live to control certain parameters of a VRChat avatar, specifically clothes toggles, over OSC.

Uses the parameter 
/TiktokLive (int)
in combination with VRChat parameter drivers in an avatar layer.
